<%= form_for(@v_user_datum) do |f| %>
  <% if @v_user_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_user_datum.errors.count, "error") %> prohibited this v_user_datum from being saved:</h2>

      <ul>
      <% @v_user_datum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
