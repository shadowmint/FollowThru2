<h1>Listing v_user_data</h1>

<table>
  <tr>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @v_user_data.each do |v_user_datum| %>
  <tr>
    <td><%= v_user_datum.user_id %></td>
    <td><%= link_to 'Show', v_user_datum %></td>
    <td><%= link_to 'Edit', edit_v_user_datum_path(v_user_datum) %></td>
    <td><%= link_to 'Destroy', v_user_datum, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New V user data', new_v_user_datum_path %>
