<%= form_for(@v_payment_type) do |f| %>
  <% if @v_payment_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_payment_type.errors.count, "error") %> prohibited this v_payment_type from being saved:</h2>

      <ul>
      <% @v_payment_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :v_user_data_id %><br />
    <%= f.number_field :v_user_data_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
